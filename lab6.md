### ActiveJob
- 功能：ActiveJob 是 Rails 的后台作业框架。它提供了一个统一的接口来声明后台作业，并可以轻松地将作业调度到不同的队列系统（如 Sidekiq、Resque 等）。
- 应用：允许开发者轻松地创建和管理后台作业，提高应用的响应能力和性能。例如，可以将发送电子邮件的任务放入后台执行。
### ActiveModel
- 功能：ActiveModel 提供了一个抽象层，使得非数据库模型（如表单对象、API 响应等）能够使用 ActiveRecord 的一些功能，如验证和序列化。
- 应用：为 Rails 应用中的模型提供了一致的接口和功能，支持对象的验证、转换和序列化，使得开发者可以轻松管理不同类型的数据模型。
### ActiveRecord
- 功能：ActiveRecord 是 Rails 的对象关系映射（ORM）库，负责将数据库表映射为 Ruby 对象。
- 应用：提供了一种简单的方式来查询和操作数据库，支持数据库迁移、关系管理和数据验证等。是 Rails 应用的核心组件之一。
### ActiveSupport
- 功能：ActiveSupport 提供了 Rails 中常用的扩展功能和工具库，包括时间、字符串、数组等类的增强方法。
- 应用：为 Rails 提供了许多辅助功能，例如时间处理、国际化支持和通知等，使得开发更加高效和便捷。
### ActionMailer
- 功能：ActionMailer 是 Rails 的邮件发送功能库，允许应用程序发送电子邮件。
- 应用：提供了一种方便的方法来创建和发送电子邮件，可以设置邮件的主题、正文、收件人等信息，并支持多种邮件发送方式。
### ActionPack
- 功能：ActionPack 是 Rails 的核心组件之一，包含了控制器和视图的处理。
- 应用：负责 HTTP 请求的处理、路由、控制器逻辑以及视图渲染，是 Rails MVC 架构中的关键部分。
### ActionView
- 功能：ActionView 是 Rails 的视图层库，负责渲染 HTML、模板和提供帮助方法。
- 应用：提供了一种生成用户界面的方式，包括视图模板、布局、表单处理等，使得开发者可以轻松构建动态的 Web 界面。